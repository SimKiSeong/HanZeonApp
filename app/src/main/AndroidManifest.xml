<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.mother">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>


    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">



        <!-- // api관련 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAR6IJxMz_EpcfVUFsvpGtRGkqS7eIslic" />

        <!-- 스플래시 액티비티 -->

        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".TodayActivity" />
        <activity
            android:name=".SchduelActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".TimmerActivity" />
        <activity
            android:name=".CalandarActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".LibraryActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".WorkFragment" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".CustomerActivity" />
    </application>

</manifest>